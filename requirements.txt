# Core
langchain>=0.1.0
qdrant-client>=1.7.0
pypdf>=3.0.0

# Optional: docling for high-quality PDF parsing (fallback to pypdf if not installed)
docling>=2.0.0

# URL document loading (OnlinePDFLoader)
langchain-community>=0.2.0

# PyMuPDF-based PDF to markdown (PyMuPDFDocumentLoader)
pymupdf4llm>=0.0.21

# Text splitters (MarkdownHeaderTextSplitter, RecursiveCharacterTextSplitter)
langchain-text-splitters>=0.2.0

# LLM adapters and embeddings (Ollama, OpenAI-compatible API)
langchain-ollama>=0.2.0
langchain-openai>=0.2.0

# API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
streamlit>=1.30.0
requests>=2.31.0
prometheus-client>=0.20.0

# Dev
pytest>=7.0.0
pytest-asyncio>=0.23.0
yapf>=0.40.0
mypy>=1.0.0
fpdf2>=2.7.0
